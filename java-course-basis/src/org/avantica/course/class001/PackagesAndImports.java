package org.avantica.course.class001;

/**
 * PACKAGES
 * =============================================
 * - Usually starts with com, org
 * - It is always declared as the first line of the classes.
 * - When we use any class (for example Random), the IDE will automatically import the package of it.
 * - java.lang is special, it does not need to be specified. (System, Integer)
 * - Specific imports
 * - Start import means import all classes from the package.
 * - Imports needs to be declared before a class.
 * - Imports should not be declared before a package - compilation error.
 */
public class PackagesAndImports {
}
